# homebrew-tap (廃止)

この tap は廃止しました。すべての cask は [tsukumistudio/tap](https://github.com/TsukumiStudio/homebrew-tap) に移管済みです。

```sh
brew untap matsufriends/tap
brew tap tsukumistudio/tap
brew install --cask tsukumistudio/tap/mornaimeter
```
